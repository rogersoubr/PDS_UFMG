#include <stdio.h>
/*uma funçao que roda por ela mesmo.
 * direta -> ela usa ela mesma
 * indireta -> ela usa outras funções.
 * Encara a recursividade como uma escada, ela sobe e em algum momento ela acaba. 
 * A cada degrau está mais perto do fim.
 * - Tudo que for recursivo, pode ser analisado como não recursivo. 
 * - A resursividade é pesada (empilhamento), para problemas que já são recursivos, é a melhor.
 * - O fim da escada vai ser onde você quer chegar ou o limite
 * - Todas tem caso base (teto/fim)  e chamada recursiva (diminui ate o fim)
 * 		- A quantidade de chamadas determina a quantidade de caso base que vai precisar.
 * - Recursividade é uma instancia da indução da matemática discreta
 * 	- indução fraca prova que se funciona em uma, vai funcionar em outra
 * 	- Indução forte fala que se chegar pegar  
 * */

//Função fatorial, usa o proprio fatorial -> n! = n*(n-1)! -> vai ser ate chegar no fatorial de 0 que é 1
// Pergunta se chegou no caso base antes de fazer a resursão 
//1) encontre padrões



int main(){
	
	
	
	return 0;
}

