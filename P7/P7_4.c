#include <stdio.h>
#include <string.h>

int main(){
    int v[100];
    int lin, col = 0;

    printf("Digite a quantidade de LINHAS: ");
    scanf("%d",&lin);

    printf("Digite a quantidade de COLUNAS");
    scanf("%d",&col);

    for (int i = 0; i < lin; i++)
    {
        fgets()
    }
    

    return 0;
}